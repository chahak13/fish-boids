function setup(){
  createCanvas(640,360)
  a = createVector(3,3);
  b = createVector(1,1);
  console.log(a.sub(b));
  console.log(a);
  arr = [1,2,33];
  index = arr.indexOf(2);
  console.log(index);
}

function draw(){
  // translate(width/2, height/2);
}
